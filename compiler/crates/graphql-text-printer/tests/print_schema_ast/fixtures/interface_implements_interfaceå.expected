==================================== INPUT ====================================
interface Actor {
  name: string
}
interface Person implements Actor {
  name: string
}
==================================== OUTPUT ===================================
interface Actor {
  name: string
}

interface Person implements Actor {
  name: string
}
